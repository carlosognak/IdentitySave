import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starterIdentitySave',
  appName: 'IdentitySave',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
